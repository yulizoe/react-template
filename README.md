# React Scaffold

## Usage

``` bash
# install dependencies
npm run install:gfw

# serve with hot reload at localhost:3000
npm run dev

# eslint
npm run lint

# stylelint
npm run lint:css

# test
npm test

# compile files for production
npm run compile

# clean and compile
npm run build

# 验证构建后的代码
npm run server
```
